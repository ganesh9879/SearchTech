void bubbleSort(vector<int>& arr, int n)
{   
    for(int i=1;i<n;i++)
    {//total rounds from 1 to n-1
        for(int j=0;j<n-i;j++)
        {
             if(arr[j]>arr[j+1])
             swap(arr[j],arr[j+1]);
        }
    }
}
